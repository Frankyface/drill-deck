# Copy to .env and fill with the project's real values (Supabase dashboard → Settings → API).
# Only the publishable key goes here — it is safe in the client; RLS is the security boundary.
EXPO_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
EXPO_PUBLIC_SUPABASE_KEY=sb_publishable_YOUR_KEY
